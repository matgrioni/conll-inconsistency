module confluent name_to_flat {
    rule r {
        match {
            GOV -[name]-> DEP
        }
        commands {
            del_edge GOV -[name]-> DEP;
            add_edge GOV -[flat:name]-> DEP;
        }
    }
}

sequences {
    main {
        name_to_flat
    }
}
