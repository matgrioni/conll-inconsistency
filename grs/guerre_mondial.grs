module confluent guerre_mondial {
    rule r(feature $phon, @lemma) {
        match {
            N1 [phon=$phon, cat=PROPN];
            N2 [phon="Guerre"];

            N2 -[amod]-> N1;
        }
        commands {
            N1.cat = ADJ;
            N1.lemma = @lemma;
            N1.Gender = Fem;
            N1.Number = Sing;
        }
    }

    #BEGIN

    Seconde##second
    Première##premier

    #END
}

sequences {
    main {
        guerre_mondial
    }
}
